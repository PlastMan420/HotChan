/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ExFormControlNameDirective } from './exFormControlName.directive';

describe('Directive: ExFormControlName', () => {
  it('should create an instance', () => {
    const directive = new ExFormControlNameDirective();
    expect(directive).toBeTruthy();
  });
});
